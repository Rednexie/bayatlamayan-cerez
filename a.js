// Define the width for alignment
const width = 20;

// First row of data
const firstRow = 'id ';
console.log(firstRow.padEnd(width, ' '), 'a');

// Second row of data
const secondRow = 'id1';
console.log(secondRow.padEnd(width, ' '),'a');
